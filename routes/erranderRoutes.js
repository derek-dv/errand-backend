const express = require(`express`);
const router = express.Router();

// Route will be configured if need be. I am leaving the below code as a placeholder
// router.post("/profile");

module.exports = router;
